import React from 'react'

export default (props) => {
  const handleLogin = () => {
    // you will change global state here
  }

  return (
    <div>
      <p>Login dulu bos</p>
      <button onClick={handleLogin}>Login</button>
    </div>
  )
}